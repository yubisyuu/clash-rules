# =====================================================================
# Stash 分流规则 (Loyalsoldier + blackmatrix7 + bgpeer)
# 适配环境：Stash (iOS / macOS / tvOS / visionOS)
# 规则模型：洋葱过滤 → 广告拒止 → 券商/加密精准注入 → 服务分流 → 国内直连 → GFW 代理 → MATCH 兜底
# =====================================================================

# =====================================================================
# [全局基础] — Stash 须拆分端口
# =====================================================================
port: 7890
socks-port: 7891
allow-lan: true
bind-address: "*"
mode: rule
log-level: warning
ipv6: false

profile:
  store-selected: true

# =====================================================================
# DNS — Stash 简化版 (不支持 fake-ip-range / cache-algorithm / listen)
# =====================================================================
dns:
  enable: true
  ipv6: false
  enhanced-mode: fake-ip
  nameserver:
    - 223.5.5.5
    - 119.29.29.29
  nameserver-policy:
    "geosite:cn,private":
      - 223.5.5.5
      - 119.29.29.29

# =====================================================================
# [Proxy Providers]
# =====================================================================
proxy-providers:
  sushi_cloud:
    type: http
    url: "YOUR_SUBSCRIPTION_URL"
    path: ./sushi_provider.yaml
    interval: 36000
    health-check:
      enable: true
      url: http://www.gstatic.com/generate_204
      interval: 300

# =====================================================================
# [Proxy Groups] — 洋葱过滤核心
# =====================================================================
proxy-groups:

  # --- 默认出口 ---
  - name: Default
    type: select
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/shadowrocket.png"
    proxies: [DIRECT, Hong Kong, Taiwan, Japan, Korea, Singapore, US, All Nodes]

  - name: AI
    type: select
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/openai.png"
    proxies: [Taiwan, Japan, US]

  - name: Instagram
    type: select
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/instagram.png"
    proxies: [Japan, Korea, US]

  - name: YouTube
    type: select
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/youtube.png"
    proxies: [Hong Kong, Taiwan, Japan, Korea, Singapore]

  - name: Google
    type: select
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/google.png"
    proxies: [Taiwan, Japan, US]

  - name: X
    type: select
    icon: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/icons/x.png"
    proxies: [Taiwan, Japan, US]

  # --- 银行与券商 ---
  - name: HSBC Hong Kong
    type: select
    icon: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/icons/hsbc_hk.png"
    proxies: [Hong Kong, Taiwan, Japan, Singapore]

  - name: Interactive Brokers
    type: select
    icon: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/icons/ibkr.png"
    proxies: [DIRECT, US]

  - name: Longbridge
    type: select
    icon: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/icons/longbridge.png"
    proxies: [Hong Kong, Taiwan, Japan, Singapore]

  - name: uSmart
    type: select
    icon: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/icons/usmart.png"
    proxies: [Hong Kong, Taiwan, Japan, Singapore]

  - name: Zinvest
    type: select
    icon: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/icons/zinvest.png"
    proxies: [Hong Kong, Taiwan, Japan, Singapore]

  - name: Phillip
    type: select
    icon: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/icons/phillip.png"
    proxies: [Hong Kong, Taiwan, Japan, Singapore]

  - name: Zircon
    type: select
    icon: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/icons/zircon.png"
    proxies: [Hong Kong, Taiwan, Japan, Singapore]

  # --- 加密货币 ---
  - name: Crypto
    type: select
    icon: "https://cdn.jsdelivr.net/gh/spothq/cryptocurrency-icons/128/color/btc.png"
    proxies: [Taiwan, Japan, Singapore]

  - name: Kraken
    type: select
    icon: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/icons/kraken.png"
    proxies: [US]

  - name: Coinbase
    type: select
    icon: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/icons/coinbase.png"
    proxies: [US]

  - name: OSL
    type: select
    icon: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/icons/osl.png"
    proxies: [Hong Kong, Taiwan, Japan, Singapore]

  # --- 开发者与游戏平台 ---
  - name: GitHub
    type: select
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/github.png"
    proxies: [Hong Kong, Korea, Singapore]

  - name: Steam
    type: select
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/steam.png"
    proxies: [DIRECT, Default, Hong Kong]

  - name: Xbox
    type: select
    icon: "https://fastly.jsdelivr.net/gh/homarr-labs/dashboard-icons/png/xbox.png"
    proxies: [DIRECT, Default, Hong Kong]

  - name: Microsoft
    type: select
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/microsoft.png"
    proxies: [DIRECT, Default]

  # --- 流媒体 ---
  - name: TikTok
    type: select
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/tiktok.png"
    proxies: [Japan, Korea]

  - name: NETFLIX
    type: select
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/netflix.png"
    proxies: [Hong Kong, Taiwan, Japan, Korea, Singapore, US]

  - name: Telegram
    type: select
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/telegram.png"
    proxies: [Hong Kong, Korea, Singapore]

  # --- 兜底 ---
  - name: Catch-All
    type: select
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/final.png"
    proxies: [DIRECT, Default, Hong Kong, Taiwan, Japan, Korea, Singapore, US]

  # --- 地理节点池（依赖 provider filter 正则过滤）---
  - name: Hong Kong
    type: url-test
    use: [sushi_cloud]
    url: "http://www.gstatic.com/generate_204"
    interval: 300
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/hk.png"
    filter: "港|HK|(?i)Hong"

  - name: Taiwan
    type: select
    use: [sushi_cloud]
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/tw.png"
    filter: "台|TW|(?i)Taiwan"

  - name: Japan
    type: select
    use: [sushi_cloud]
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/jp.png"
    filter: "日|东京|JP|(?i)Tokyo|(?i)Japan"

  - name: Korea
    type: url-test
    use: [sushi_cloud]
    url: "http://www.gstatic.com/generate_204"
    interval: 300
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/kr.png"
    filter: "韩|首尔|KR|(?i)Korea"

  - name: Singapore
    type: url-test
    use: [sushi_cloud]
    url: "http://www.gstatic.com/generate_204"
    interval: 300
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/sg.png"
    filter: "Singapore|坡|狮城|SG|(?i)Singapore"

  - name: US
    type: select
    use: [sushi_cloud]
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/us.png"
    filter: "美|US|States|(?i)America"

  - name: All Nodes
    type: url-test
    use: [sushi_cloud]
    icon: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script/icon/color/global.png"
    exclude-filter: "续费|到期|剩余|官网|提示|通知|公告|网站|地址"

# =====================================================================
# Rule Providers — YAML 锚点已展开 (Stash 不兼容 Clash Meta 锚点语法)
# =====================================================================
rule-providers:

  # 广告
  bm_ad:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script@master/rule/Clash/Advertising/Advertising.yaml"

  # 券商（bgpeer）
  bm_hsbc:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/hsbc/hsbc.yaml"

  bm_ibkr:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/ibkr/ibkr.yaml"

  bm_longbridge:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/longbridge/longbridge.yaml"

  # Coinbase
  bm_coinbase:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/coinbase/coinbase.yaml"

  # OSL
  bm_osl:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/osl/osl.yaml"

  # 券商户内联
  bm_usmart:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/usmart/usmart.yaml"

  bm_zinvest:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/zinvest/zinvest.yaml"
  bm_phillip:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/phillip/phillip.yaml"

  # 加密货币交易所
  bm_kraken:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/kraken/kraken.yaml"

  # AI 服务
  bm_gemini:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/Gemini/Gemini.yaml"

  bm_openai:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/OpenAI/OpenAI.yaml"

  # 加密货币综合
  bm_crypto:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/Crypto/Crypto.yaml"

  # 社交媒体与流媒体
  bm_tiktok:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/TikTok/TikTok.yaml"

  bm_netflix:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/Netflix/Netflix.yaml"

  bm_telegram:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/Telegram/Telegram.yaml"

  bm_instagram:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/Instagram/Instagram.yaml"

  bm_youtube:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/YouTube/YouTube.yaml"

  bm_google:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/Google/Google.yaml"

  bm_x:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/X/X.yaml"

  # 开发者平台
  bm_github:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/GitHub/GitHub.yaml"

  bm_docker:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/Docker/Docker.yaml"

  # 游戏平台
  bm_steam:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/Steam/Steam.yaml"

  bm_xbox:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/Xbox/Xbox.yaml"

  bm_microsoft:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/Microsoft/Microsoft.yaml"

  bm_nintendo:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://cdn.jsdelivr.net/gh/yubisyuu/clash-rules@main/rules/Nintendo/Nintendo.yaml"


  # Loyalsoldier — 国内直连 / GFW / Apple / 中国 IP
  ls_lan:
    type: http
    behavior: ipcidr
    format: text
    interval: 86400
    url: "https://fastly.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/lancidr.txt"

  ls_direct:
    type: http
    behavior: domain
    format: text
    interval: 86400
    url: "https://fastly.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/direct.txt"

  ls_apple:
    type: http
    behavior: domain
    format: text
    interval: 86400
    url: "https://fastly.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/apple.txt"

  ls_proxy:
    type: http
    behavior: domain
    format: text
    interval: 86400
    url: "https://fastly.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/proxy.txt"

  ls_gfw:
    type: http
    behavior: domain
    format: text
    interval: 86400
    url: "https://fastly.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/gfw.txt"

  ls_cncidr:
    type: http
    behavior: ipcidr
    format: text
    interval: 86400
    url: "https://fastly.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/cncidr.txt"

# =====================================================================
# [规则匹配逻辑 - 九层洋葱过滤模型]
# =====================================================================
rules:
  # 第一层：局域网与进程绕过
  - RULE-SET,ls_lan,DIRECT,no-resolve

  # 第二层：广告屏蔽
  - RULE-SET,bm_ad,REJECT
  - DOMAIN-SUFFIX,ut.taobao.com,REJECT
  - DOMAIN-SUFFIX,bugly.qq.com,REJECT

  # 第三层：银行与券商 — bgpeer RULE-SET + 关键域内联兜底
  - RULE-SET,bm_hsbc,HSBC Hong Kong
  - DOMAIN-SUFFIX,hsbcbroker.com,HSBC Hong Kong
  - DOMAIN-SUFFIX,securities.hsbc.com.hk,HSBC Hong Kong
  - RULE-SET,bm_ibkr,Interactive Brokers
  - RULE-SET,bm_longbridge,Longbridge

  # 第四层：加密货币交易所（须在泛 Crypto 之前，防止覆盖）
  - RULE-SET,bm_coinbase,Coinbase
  - RULE-SET,bm_kraken,Kraken
  - RULE-SET,bm_osl,OSL

  # 第五层：券商（手动内联域）
  - RULE-SET,bm_usmart,uSmart
  - RULE-SET,bm_zinvest,Zinvest
  - RULE-SET,bm_phillip,Phillip
  - DOMAIN-SUFFIX,zr.hk,Zircon
  - DOMAIN-SUFFIX,zr66.hk,Zircon
  - DOMAIN-SUFFIX,zrsecbk.com,Zircon

  # 第六层：AI 服务
  - RULE-SET,bm_gemini,AI
  - RULE-SET,bm_openai,AI

  # 第七层：泛加密货币综合
  - RULE-SET,bm_crypto,Crypto

  # 第八层：社交媒体与流媒体
  - RULE-SET,bm_tiktok,TikTok
  - RULE-SET,bm_netflix,NETFLIX
  - RULE-SET,bm_telegram,Telegram
  - RULE-SET,bm_instagram,Instagram
  - RULE-SET,bm_youtube,YouTube
  - RULE-SET,bm_google,Google
  - RULE-SET,bm_x,X

  # 第九层：开发者工具与游戏平台
  - RULE-SET,bm_github,GitHub
  - RULE-SET,bm_docker,Default
  - RULE-SET,bm_steam,Steam
  - RULE-SET,bm_xbox,Xbox
  - RULE-SET,bm_microsoft,Microsoft
  - RULE-SET,bm_nintendo,DIRECT

  # 巴哈姆特（台湾站）
  - DOMAIN-SUFFIX,gamer.com.tw,Taiwan

  # Apple 服务 → 国内直连
  - RULE-SET,ls_apple,DIRECT

  # 国内域名 → 直连
  - RULE-SET,ls_direct,DIRECT

  # GFW / 代理列表 → 兜底策略
  - RULE-SET,ls_gfw,Default
  - RULE-SET,ls_proxy,Default

  # 中国 IP → 直连（no-resolve）
  - RULE-SET,ls_cncidr,DIRECT,no-resolve

  # 终局
  - MATCH,Catch-All
